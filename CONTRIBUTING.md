# 怎么在这个仓库里工作

团队怎么协作见 [协作机制](00-团队协作/协作机制.md)。这里只讲仓库操作。

## 这里放什么

项目管理与对齐材料。**不放代码**——服务端与 Expert 包各在自己的仓库。

也不放：凭据、上游接口的内部契约、平台规范原件、实测原始产物。`.gitignore` 已覆盖，提交前确认 `git status` 干净。

## 提交信息

写清楚改了什么、为什么。

推翻已有结论的改动，同时更新 [决策记录](00-团队协作/决策记录/)，别让旧结论留在别处继续被引用。

## 写文档的三条要求

**短。** 用图和表承载结构，正文只写图表表达不了的。压缩时先列必保清单——具体标识、口径数字、执行约束、失败模式一个都不能删。

**事实带来源。** 涉及数据或接口的论断，附上能复核的位置或查询语句。做不到就标「待验证」。

**不重复定义。** 同一个事实只在一处定义，别处引用。发现两处说法不一致，停下来追究哪个错了。
